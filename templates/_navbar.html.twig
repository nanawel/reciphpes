<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('index') }}">
        <img src="{{ asset('build/images/logo.svg') }}" id="navbar-logo" alt="Logo">
        Reciphpes!
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_recipe') }}"><i class="fas fa-pizza-slice"></i>Recettes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_ingredient') }}"><i class="fas fa-carrot"></i>Ingrédients</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_location') }}"><i class="fas fa-book"></i>Emplacements</a>
            </li>
        </ul>
        <div>
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-plus-circle"></i>Créer...
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('app_recipe_new') }}"><i class="fas fa-pizza-slice"></i>Recette</a>
                        <a class="dropdown-item" href="{{ path('app_ingredient_new') }}"><i class="fas fa-carrot"></i>Ingrédient</a>
                        <a class="dropdown-item" href="{{ path('app_location_new') }}"><i class="fas fa-book"></i>Emplacement</a>
                        <!--
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>
                        -->
                    </div>
                </li>
                <li>
                    <form class="form-inline my-2 my-lg-0" action="{{ path('search_results') }}" method="get">
                        <input class="form-control mr-sm-2" type="search" placeholder="Rechercher"
                               aria-label="Rechercher" name="q">
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit"><i class="fas fa-search"></i>Rechercher
                        </button>
                    </form>
                </li>
            </ul>
        </div>
    </div>
</nav>
