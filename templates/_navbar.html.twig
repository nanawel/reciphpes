<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('index') }}">
            <img src="{{ asset('build/images/logo.svg') }}" id="navbar-logo" alt="Logo">
            Reciphpes!
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_recipe') }}">
                        <i class="fas fa-pizza-slice"></i>{% trans %} Recipes {% endtrans %}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_ingredient') }}">
                        <i class="fas fa-carrot"></i>{% trans %} Ingredients {% endtrans %}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_location') }}">
                        <i class="fas fa-book"></i>{% trans %} Locations {% endtrans %}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_tag') }}">
                        <i class="fas fa-tag"></i>{% trans %} Tags {% endtrans %}</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                {% if access_manager.isRestrictedAccessEnabled() %}
                    {% if not access_manager.isSignedIn() %}
                        <li class="nav-item">
                            <a class="btn btn-outline-secondary me-sm-2" href="{{ path('signin') }}"><i
                                        class="fas fa-sign-in-alt"></i>{% trans %} Sign In {% endtrans %}</a>
                        </li>
                    {% endif %}
                    {% if access_manager.hasWriteAccess() %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown"
                               data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-plus-circle"></i>{% trans %} New... {% endtrans %}
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_recipe_new') }}"><i
                                                class="fas fa-pizza-slice"></i>{% trans %} Recipe {% endtrans %}</a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_ingredient_new') }}"><i
                                                class="fas fa-carrot"></i>{% trans %} Ingredient {% endtrans %}</a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_location_new') }}"><i
                                                class="fas fa-book"></i>{% trans %} Location {% endtrans %}</a>
                                </li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_recipe_masscreate') }}"><i
                                                class="fas fa-pizza-slice"></i>{% trans %} Recipe (batch) {% endtrans %}
                                    </a>
                                </li>
                            </ul>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>
            <form class="d-flex" action="{{ path('app_search_results') }}" method="get">
                <input class="form-control me-sm-2" type="search"
                       placeholder="{% trans %} Search {% endtrans %}"
                       aria-label="Rechercher" name="q">
            </form>
            {% if access_manager.isRestrictedAccessEnabled() %}
                {% if access_manager.isSignedIn() %}
                    <ul class="navbar-nav">
                        <li class="nav-item me-auto">
                            <a class="btn btn-outline-secondary mx-sm-2" href="{{ path('signout') }}">
                                <i class="fas fa-sign-out-alt me-0"></i>
                            </a>
                        </li>
                    </ul>
                {% endif %}
            {% endif %}
        </div>
    </div>
</nav>
