{% extends 'content.html.twig' %}

{% block content %}
    <div class="btn-toolbar float-right" role="toolbar" aria-label="Toolbar">
        <div class="btn-group mr-2" role="group" aria-label="Actions">
            <a href="{{ path('app_location_new') }}" class="btn action btn-secondary"><i class="fas fa-plus-circle"></i>
                <span>Créer</span></a>
        </div>
    </div>
    <h1><i class="fas fa-map-marked-alt"></i> Emplacements</h1>

    {% include '_grid.html.twig' %}
{% endblock %}
